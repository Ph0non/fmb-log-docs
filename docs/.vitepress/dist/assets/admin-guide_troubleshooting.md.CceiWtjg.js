import{_ as n,c as i,o as t,ag as r}from"./chunks/framework.dvv-DFtf.js";const p=JSON.parse('{"title":"Troubleshooting","description":"","frontmatter":{},"headers":[],"relativePath":"admin-guide/troubleshooting.md","filePath":"admin-guide/troubleshooting.md","lastUpdated":1768091954000}'),o={name:"admin-guide/troubleshooting.md"};function a(l,e,s,d,h,u){return t(),i("div",null,[...e[0]||(e[0]=[r('<h1 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h1><h2 id="app-schließt-sich-ohne-meldung-„crash" tabindex="-1">App schließt sich ohne Meldung / „Crash“ <a class="header-anchor" href="#app-schließt-sich-ohne-meldung-„crash" aria-label="Permalink to &quot;App schließt sich ohne Meldung / „Crash“&quot;">​</a></h2><p>Im Production‑Build läuft Rust mit <code>panic = &quot;abort&quot;</code>. Das bedeutet: Ein Panic beendet die Anwendung sofort, ohne dass ein Fehlerdialog angezeigt wird.</p><p>FMB Log schreibt deshalb eine Logdatei mit Crash‑Hinweisen und Backtrace:</p><ul><li>Öffnen über <strong>Administration → Einstellungen → Diagnose → Log öffnen</strong></li><li>Pfad (Windows): <code>%LOCALAPPDATA%\\\\com.ewn.fmblog\\\\logs\\\\fmb-log.log</code></li></ul><p>Wenn die Anwendung beim Start erkennt, dass der letzte Start unerwartet endete, erscheint zusätzlich ein Hinweis auf der Anmeldeseite.</p><h2 id="„diese-seite-funktioniert-nur-in-der-tauri-desktop-app" tabindex="-1">„Diese Seite funktioniert nur in der Tauri-Desktop-App“ <a class="header-anchor" href="#„diese-seite-funktioniert-nur-in-der-tauri-desktop-app" aria-label="Permalink to &quot;„Diese Seite funktioniert nur in der Tauri-Desktop-App“&quot;">​</a></h2><p>Einige Funktionen (Dateidialoge, Zugriff auf Ressourcenpfade, PDF‑Export, Datenbankzugriff) benötigen Tauri‑APIs. Wenn Sie die Web‑UI im Browser starten (z. B. mit <code>pnpm dev</code>), sind diese APIs nicht verfügbar und die Anwendung zeigt entsprechend einen Hinweis.</p><p>Für Desktop-Tests:</p><ul><li><code>pnpm tauri dev</code></li></ul><h2 id="export-dateidialoge-funktionieren-nicht" tabindex="-1">Export/Dateidialoge funktionieren nicht <a class="header-anchor" href="#export-dateidialoge-funktionieren-nicht" aria-label="Permalink to &quot;Export/Dateidialoge funktionieren nicht&quot;">​</a></h2><p>Dateidialoge und das Öffnen/exportieren von Dateien funktionieren nur in der Desktop‑App und sind zusätzlich durch das Tauri‑Permissions‑System abgesichert. Wenn Sie eine Meldung wie „not allowed“ oder „Permission … not found“ sehen, prüfen Sie:</p><ul><li>ob Sie die Funktion in der Desktop‑App ausführen (nicht im Browser),</li><li>ob das passende Plugin (z. B. <code>opener</code>) aktiviert ist,</li><li>ob die Berechtigung in <code>src-tauri/capabilities/default.json</code> erlaubt ist (z. B. <code>opener:allow-open-path</code>).</li></ul><h2 id="netzwerk-db-sporadische-locks-fehler" tabindex="-1">Netzwerk-DB: sporadische Locks/Fehler <a class="header-anchor" href="#netzwerk-db-sporadische-locks-fehler" aria-label="Permalink to &quot;Netzwerk-DB: sporadische Locks/Fehler&quot;">​</a></h2><p>Wenn die Datenbank auf einem Netzlaufwerk liegt, können sporadische Sperren oder Zugriffsfehler auftreten. Ursache ist meist das Zusammenspiel aus SMB‑Locking, OpLocks und gleichzeitigen Schreibzugriffen. Typische Maßnahmen:</p><p>Prüfen:</p><ul><li>Schreibrechte im Zielordner und Stabilität der Verbindung</li><li>Share/SMB‑Konfiguration (Locking/OpLocks)</li><li>ob mehrere Nutzer gleichzeitig schreiben (z. B. paralleler Import)</li></ul><p>Wenn Fehler reproduzierbar unter Last auftreten, ist ein lokaler DB‑Pfad pro Nutzer die robustere Variante.</p><h2 id="protokoll-kann-nicht-geladen-werden" tabindex="-1">Protokoll kann nicht geladen werden <a class="header-anchor" href="#protokoll-kann-nicht-geladen-werden" aria-label="Permalink to &quot;Protokoll kann nicht geladen werden&quot;">​</a></h2><p>Wenn beim Öffnen eines Messprotokolls eine Fehlermeldung erscheint, sind die häufigsten Ursachen:</p><ol><li><p><strong>Hub nicht erreichbar</strong> (Netzlaufwerk offline / Pfad falsch)<br> Protokolle werden im Hub‑Ordner als Packfiles unter <code>protocols/…</code> gespeichert. Ist der Hub nicht erreichbar, kann die App Protokolle nur laden, wenn sie bereits im lokalen Cache liegen.</p></li><li><p><strong>Offline‑Import auf anderem Rechner</strong><br> Wurde eine Messung importiert, während der Hub nicht erreichbar war, ist das Protokoll zunächst nur auf dem importierenden Rechner im lokalen Cache verfügbar. Der Upload in <code>protocols/…</code> wird automatisch nach dem nächsten erfolgreichen Sync nachgeholt. Bis dahin können andere Clients die Messung sehen, das Protokoll aber nicht öffnen.</p></li><li><p><strong>Lokaler Cache wurde geleert</strong><br> Wenn ein Protokoll noch nicht in das Hub‑Archiv hochgeladen war und der lokale Cache gelöscht wurde, kann es ggf. nicht mehr automatisch hochgeladen werden. In diesem Fall muss das Protokoll erneut importiert werden (oder aus einem Backup wiederhergestellt werden).</p></li></ol><h2 id="import-hangt-import-beendet-nicht" tabindex="-1">Import hängt / Import beendet nicht <a class="header-anchor" href="#import-hangt-import-beendet-nicht" aria-label="Permalink to &quot;Import hängt / Import beendet nicht&quot;">​</a></h2><p>Wenn ein Import scheinbar „endlos“ läuft, zeigt die Import‑Seite unter dem Button <strong>Importieren</strong> den aktuellen Schritt (z. B. „Schritt: Protokoll speichern…“). Das hilft, den Engpass einzugrenzen.</p><p>FMB Log verwendet außerdem Timeouts, damit ein Import nicht unbegrenzt hängen bleibt. Falls eine Zeitüberschreitung auftritt, enthält die Fehlermeldung den betroffenen Schritt.</p><p>Typische Ursachen je Schritt:</p><ul><li><strong>„Protokoll speichern…“</strong>: Zugriff auf den Hub‑Ordner (Schreibrechte/SMB/Antivirus) oder Initialisierung der zstd‑Kompression.</li><li><strong>„Gebinde signieren…“ / „Signatur (neu/alt)…“</strong>: Stronghold‑Vault nicht erreichbar (liegt im Hub‑Ordner) oder falsches Signier‑/Vault‑Passwort.</li><li><strong>„Messung speichern…“</strong>: lokale Replica‑DB ist blockiert (z. B. durch parallele Aktionen) oder Hub/Sync verursacht Locking unter Last.</li></ul><p>Für die Diagnose ist die Logdatei hilfreich:</p><ul><li>Öffnen über <strong>Administration → Einstellungen → Diagnose → Log öffnen</strong></li><li>Pfad (Windows): <code>%LOCALAPPDATA%\\\\com.ewn.fmblog\\\\logs\\\\fmb-log.log</code></li></ul><div class="info custom-block"><p class="custom-block-title">Kurzcheck</p><ul><li>Browser‑Modus (<code>pnpm dev</code>) eignet sich für UI‑Layout, nicht für Tauri‑Funktionen.</li><li>Desktop‑Tests immer mit <code>pnpm tauri dev</code>.</li><li>„not allowed“ deutet meist auf fehlende Tauri‑Permissions hin.</li></ul></div>',29)])])}const g=n(o,[["render",a]]);export{p as __pageData,g as default};
